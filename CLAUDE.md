# home 작업 규칙

부부 둘이 쓰는 사이트. **영역**을 여러 개 담는다.

| 영역 | 무엇 | 경로 |
|---|---|---|
| 가계부 | 월 계획 · 생활비 기록 · 대출 상환표 | `/wallet` |
| 일정 | 부부 각자의 캘린더를 끌어와 한 곳에서 본다. **조회만** | `/schedule` |

문서는 셋이다.

| 파일 | 무엇 | 커밋 |
|---|---|---|
| `CLAUDE.md` | 규칙·컨벤션·설계 결정. 작업 전에 읽는다 | O |
| `CLAUDE.local.md` | 인프라 식별자와 실제 금액 | **X** (리포가 public) |
| `README.md` | 사람이 읽는 소개와 명령어 | O |

## 가계부가 무엇인가

세 곳에 흩어져 있던 것을 합친다.

1. **월 계획** - 엑셀 시트를 대신한다. 항목 → 세부항목 2단, 결제일, 지급 수단, 기간
2. **생활비 기록** - 가계부 앱을 대신한다. **유동 생활비만** 기록한다
3. **대출 상환표** - 은행이 준 표를 그대로 넣고 계획에 그 달 값을 제안한다

핵심 문장은 하나다. **"계획은 월 단위로 세우고, 생활비만 매일 기록한다."**

셋을 잇는 지점도 셋이다.

```
        ┌─ 대출 상환표 ──→ 계획의 원리금 금액 (제안, 탭해서 채움)
계획 ───┤
        └─ 계획의 '생활비 유동' ──→ 일 기록의 이번 달 예산
                                      ↓
                                 홈의 결론 - 얼마 남았나
```

**지급 수단이 셋을 가로로 잇는다.** 계획의 그룹마다 어느 카드·계좌에서 빠져나가는지가
붙어 있고, 기록도 같은 목록에서 고른다. 설정에서 이름을 바꾸면 둘이 함께 따라온다.

**두 화면의 결론이 다르다.** 계획 화면은 **잔액**(수입 − 지출), 홈은 **이번 달
생활비**(예산 대비 얼마 썼나). 같은 숫자를 두 화면에서 크게 반복하지 않는다.
화면에 결론이 둘이면 무엇을 보고 판단해야 하는지 흐려진다.

## 영역을 하나 붙이려면

`lib/areas.ts`의 표에 한 줄을 더하고 `app/<slug>/` 아래에 그 탭들의 페이지를 만든다.
상단 스위치와 하단 네비와 서버 재검증 경로가 전부 이 표를 읽으므로 다른 데는 고칠 곳이 없다.

## 깨뜨리면 안 되는 것

**tium 자원을 건드리지 않는다.** EC2 호스트와 RDS 인스턴스만 공유하고 나머지는 전부
분리한다 - 전용 DB·전용 계정·전용 도커 네트워크·전용 보안그룹·`/home/prod/*` SSM.
언제든 지워도 tium에 흔적이 남지 않아야 한다(`infra/teardown-home.sh`).

**`query()`는 준비된 문장(`execute`)이다.** `IN (?)`에 배열을 넣으면 펼쳐지지 않고
**조용히 0행**이 된다. 오류도 안 난다. 목록 조건이 필요하면 DB에서 읽은 정수를 문자열로
이어 붙인다(`lib/plan.ts`의 적립 계산이 그 예다).

**조인을 추가하면 파라미터 순서를 다시 센다.** 플레이스홀더는 SQL 텍스트의 등장 순서로
채워진다. `plan_amount` 조인을 중간에 끼우면서 뒤의 값이 한 칸씩 밀려 `book_id` 자리에
월 문자열이 들어갔고, 계획 화면이 통째로 비었다. 한 번 겪었다.

**빌드 시점에 DB에 붙지 않는다.** 커넥션 풀은 첫 쿼리 때 만든다(`lib/db.ts`의 `getPool`).
모듈 로드 시점에 만들면 `next build`가 라우트 설정을 수집하며 파일을 평가할 때
`DATABASE_URL`이 없어 CI와 Docker 빌드가 깨진다.

**`proxy.ts`의 matcher에서 정적 자산을 빼둔 것을 되돌리지 않는다.** 이미지 최적화기가
`public/` 파일을 자기 자신에게 다시 요청하는데 그 내부 요청에는 `x-origin-verify`
헤더가 없다. 막으면 이미지가 깨지고, 로컬에서는 절대 재현되지 않는다.

**바깥으로 나가는 주소를 `request.url`로 만들지 않는다.** CloudFront가 EC2:3001로
넘길 때 컨테이너가 보는 요청은 `http://`다. 그걸로 OAuth 리디렉션 주소를 만들면
구글에 `http://home.devckm.kr/...`이 가고, 구글은 localhost가 아닌 http를 정책
위반으로 막는다(`400 invalid_request`). **로컬 dev는 원래 http라 절대 재현되지 않는다.**
`lib/google.ts`의 `siteUrl()`을 쓴다 - `AUTH_URL`만 바깥 주소를 안다. 한 번 겪었다.

**푸시 전에 Docker로 빌드해본다.** CI를 디버거로 쓰면 한 번에 3분씩 태운다.

```shell
docker build -t home:verify .
docker run --rm -p 3101:3001 -e DATABASE_URL=… -e ORIGIN_VERIFY_SECRET=아무값 home:verify
```

`ORIGIN_VERIFY_SECRET`을 **반드시 넣고** 띄운다. 로컬 dev에는 없어서 검증이 no-op이고,
그래서 오리진 검증과 얽힌 버그는 로컬에서 절대 재현되지 않는다.

**lockfile은 리눅스에서 만든다.**
`docker run --rm -v "$PWD":/w -w /w node:22-alpine npm install --package-lock-only`

**허용 목록이 비면 아무도 통과하지 못한다.** `ALLOWED_KAKAO_IDS`를 옵셔널하게
바꾸지 않는다. 값을 실수로 안 넣었을 때 전부 열리는 쪽보다 전부 막히는 쪽이 안전하다.
`*`로 두면 카카오 계정이 있는 누구나 급여·대출 데이터에 들어온다.

**허용 목록은 요청마다 검사한다.** `signIn` 콜백은 로그인하는 순간에만 돈다.
그것만 믿으면 목록에서 뺀 계정의 **이미 발급된 세션 쿠키가 만료될 때까지 계속 통과한다.**
페이지와 서버 액션은 전부 `currentUser()`를 거친다 - 이 검사를 빼지 않는다.

**카카오 회원번호는 앱마다 다르다.** 같은 계정이라도 handari 앱과 home 앱에서 서로 다른
번호를 받는다(앱 그룹을 묶지 않았으면). 다른 서비스의 DB에서 가져다 쓸 수 없고,
**이 앱으로 한 번 로그인해서 로그에 찍힌 번호**를 써야 한다. 그래서 `signIn` 콜백은
통과든 거절이든 번호를 로그에 남긴다.

**로컬에서는 DB를 쓰지 않는다.** 개발 DB가 따로 없고 RDS의 같은 `home` 데이터베이스가
곧 서비스 데이터라서, 로컬에서 띄워 만지면 테스트 데이터가 그대로 서비스에 보인다.
동작 확인은 서버에서 한다.

터널(`npm run tunnel`)은 **마이그레이션과 상환표 임포트가 필요할 때만** 연다.
`npm run typecheck`·`lint`·`build`와 `docker build`는 DB 없이 돌아간다 -
푸시 전 검증은 그것으로 한다.

## 문서를 코드와 같이 고친다

개발이 끝나면 이 세 문서를 **같은 커밋에서** 손본다. 나중에 몰아서 고치면
코드가 앞서 있는 문서를 읽고 잘못 판단하게 된다.

- `CLAUDE.md` - 규칙이나 설계 결정이 늘거나 바뀌었으면
- `CLAUDE.local.md` - 식별자·자격·실제 값이 늘었으면
- `README.md` - 영역·명령어·경로가 바뀌었으면

## 글쓰기 컨벤션

**`-`(em dash)를 쓰지 않는다.** 곁말은 ` - `(하이픈)으로 붙인다. 코드 주석·문서·화면
문구 전부 같다.

**화면에 설명 문단을 두지 않는다.** 조작법은 해보면 알게 된다. 색이나 기호가 무엇을
뜻하는지만 짧은 범례로 두고, 그마저 문장이 아니라 그 모양을 그대로 보여준다.

## 설계에서 지키는 결정

### 메뉴와 이동

- **단은 둘까지다.** 영역 → 화면이 끝이고 그 아래는 시트로 연다. 하루 2.4건을 넣는
  작업이라 한 단을 더 파면 입력이 그만큼 느려진다
- **하단 네비를 영역 목록으로 쓰지 않는다.** 네 칸뿐이라 영역이 다섯 개가 되는 순간
  `더보기` 뒤로 밀리고, 거기 들어간 영역은 안 쓰게 된다. 영역은 상단 왼쪽 스위치가
  맡고 하단 네 칸은 지금 영역의 화면이 통째로 쓴다
- **영역 스위치는 영역이 둘 이상일 때만 나온다.** 고를 것이 하나뿐이면 눌러도 아무 일이
  없는 버튼이 하나 늘 뿐이다. 표에 한 줄을 더하면 그때 나타난다
- **영역은 URL 첫 칸이다**(`/wallet/plan`). 루트에는 화면을 두지 않고 마지막으로 본
  영역(쿠키 `wa`, `proxy.ts`가 적는다)으로 보낸다 - 한 영역을 루트에 두면 그 영역만
  특별해져서 나중에 빼거나 순서를 바꿀 때 걸린다
- **월은 한 영역 안에서 화면 전체가 하나를 공유한다**(쿠키 `wm`, URL의 `?m=`이 우선).
  화면별로 나누면 홈은 8월, 계획은 6월처럼 갈려서 어느 달을 보는지 헷갈린다.
  월 선택은 `axis: 'month'` 인 영역에만 뜬다 - 달을 안 쓰는 영역에 월이 떠 있으면
  그것이 무엇을 바꾸는 건지 알 수 없다
- **화면 맨 위는 한 줄이다.** 왼쪽에 고르는 것들(영역, 그 영역이 달을 쓰면 월),
  오른쪽에 설정. 화면 이름은 하단 탭바가 맡는다
- **월 이동에 좌우 화살표 버튼을 쓰지 않는다.** 이름을 탭해 시트에서 고른다.
  화살표가 뒤로 가기와 모양이 겹쳐서 헷갈린다. **달력을 좌우로 미는 것은 둔다** -
  미는 동작에는 그 문제가 없다. 세로로 더 많이 움직였으면 무시한다,
  안 그러면 스크롤하려다 달이 넘어간다
- **`오늘` 버튼은 이번 달이 아닐 때만 나온다.** 이미 오늘이면 눌러도 아무 일이
  없는 버튼이다. 영역 스위치를 하나뿐일 때 안 띄우는 것과 같은 규칙이다
- **시트가 열려 있는 동안 하단 네비를 내린다.** z-index로는 안 된다 - 네비의
  `backdrop-filter`가 iOS에서 그 요소를 합성 레이어로 올려서, 시트가 더 위(41 > 30)인데도
  네비가 위에 그려진다. 월 격자의 마지막 줄이 가려지던 것이 그것이다

### 화면

- **입력과 편집은 전부 바텀시트다.** 별도 화면으로 밀어내지 않는다 - 화면 이동이
  없으면 뒤로 가기와 월 이동이 헷갈릴 일도 없다
- **계획 화면에는 두 종류의 줄만 있다.** 회색 카드 = 그룹(탭 = 펼치기, 금액은 합계),
  흰 바탕 = 항목(이름 탭 = 상세, 금액칸 = 바로 고침). 예외를 두면 헷갈린다
- **인라인 금액 편집은 그룹 안 세부항목에서만.** 항목 줄은 줄 전체가 시트를 여는
  버튼이라 그 안에 입력칸을 또 두면 헷갈린다
- **목록 한 줄의 각 영역은 고정 폭이다.** 값이 비어도 다음 칸이 밀리지 않아야 눈이
  세로로 훑을 수 있다
- **설정에는 바꿀 수 있는 것만 둔다.** 읽기만 하는 칸을 두면 무엇을 눌러야 하는지
  헷갈린다. 대출 상환표는 스크립트로 넣으므로 설정에 두지 않는다
- **설정은 영역마다 따로다**(`/wallet/settings`, `/schedule/settings`). 한 화면에
  섞으면 영역이 늘수록 잡동사니 서랍이 되고 무엇이 어느 영역 것인지 알 수 없게 된다.
  상단 톱니는 **지금 영역의** 설정을 열고, `완료`는 그 영역으로 돌아간다
- **삭제는 확인 모달 없이 지우고 5초간 되돌리기를 띄운다**
- **카테고리는 일곱 개다.** 늘리지 않는다 - 하루 2.4건을 넣는 작업이라 격자에 칸을
  더하면 입력이 그만큼 느려진다. 임포트한 지출을 세어 보니 식비 하나가 건수의 3분의
  1이었고 하위 넷은 28개월 동안 합쳐도 얼마 안 되면서 격자에서 자리를 차지했다
  (분포는 `CLAUDE.local.md`)
- **차트 라이브러리를 넣지 않는다.** 서버에서 SVG를 그린다. Recharts/Chart.js는
  gzip 후 100~200KB인데 그릴 것은 네 종류고 상호작용은 툴팁 정도다. 서버에서 그리면
  클라이언트 JS가 0이 된다
- **원형 차트를 쓰지 않는다.** 비중 비교는 정렬된 가로 막대가 정확하다.
  두 달치도 없을 때 추이를 그리지 않는다 - 빈 축은 노이즈다
- 색은 둘뿐이다 - **연두**는 조작과 정상, **빨강**은 지출과 초과. 수입에 색을 쓰지 않는다.
  연두가 이미 조작색이라 초록 계열을 정보에 또 쓰면 무엇이 버튼인지 구분이 안 된다

### 일정

- **조회만 한다.** 여기서 만들거나 고치지 않는다. 원본은 각자의 구글·네이버에 있고
  우리는 비추기만 한다. 그래서 `[+]`를 두지 않는다 - 눌러놓고 "왜 안 만들어지지"
  하게 된다
- **배치를 만들지 않는다.** 크론 대신 화면을 열 때 마지막 동기화가 24시간을 넘었으면
  그때 가져온다. 부부 둘이 하루에 한 번은 여는 화면이라 결과는 같고 인프라가 안 는다.
  새로고침 버튼이 '지금 당장' 경로다
- **한 번에 두 해를 채운다**(보는 해와 그다음 해). 한 해만 채우면 12월에 다음 달을
  볼 때 비고 해가 바뀌는 순간 통째로 빈다. `synced_year`가 앞의 해를 적어두고,
  그 밖을 보면 다시 가져온다 - `synced_at`만으로는 해가 바뀐 것을 알 수 없다
- **일정을 한 번에 넣는다.** 한 건씩 넣으면 왕복이 건수만큼 생긴다. 두 해치면
  800건이 넘어서, 가져오는 데 1초도 안 걸리는 일이 넣는 데 10초가 된다.
  이 동기화는 화면 렌더 안에서 돌기 때문에 그 시간이 그대로 흰 화면이 된다
- **한 소스의 것을 통째로 갈아끼운다.** 창 안만 지우면 다른 해를 봤던 흔적이 남는다 -
  달력을 밀어 2029년까지 갔다 돌아오면 2029·2030이 그대로 쌓인다. 우리가 보여주는
  것은 언제나 방금 채운 두 해뿐이라 그 밖은 쓰레기고, 남겨두면 원본에서 지운 일정이
  거기 살아 있다
- **반복 일정을 우리가 풀지 않는다.** 구글에 `singleEvents=true`로 물으면 이미
  펼쳐서 준다. RRULE을 직접 해석하면 예외 규칙에서 원본과 어긋나고 그 차이를 설명할
  방법이 없다. 상환표를 계산하지 않는 것과 같은 이유다
- **구글 종일 일정은 끝 날짜가 하루 뒤로 온다.** 넣을 때 하루를 뺀다. 안 그러면
  격자에서 하루가 더 칠해진다
- **동기화 실패를 조용히 넘기지 않는다.** 옛 일정을 보여주면서 아무 말도 안 하는 것이
  최악이다. `sync_error`를 화면에 그대로 띄운다
- **사람은 색으로 구분하고 이름은 합쳐 볼 때만 붙인다.** 색은 격자를 훑을 때 쓰고
  이름은 목록을 읽을 때 쓴다. 한 사람만 보고 있으면 매 줄에 같은 이름이 반복될 뿐이다
- **범례가 곧 필터다.** 색이 무엇을 뜻하는지 보여주면서 누르면 그 사람만 남는다.
  연결이 하나뿐이면 필터를 두지 않는다 - 눌러도 아무 일이 없는 버튼이 된다
- **카카오에 구글을 얹지 않는다.** 카카오는 로그인이고 구글은 캘린더 읽기 권한이라
  하는 일이 다르다. 섞으면 "구글로도 로그인되나?"가 되고 허용 목록이 무너진다
- **네이버 CalDAV는 두 단계다.** `calendar-query`는 주소 목록만 주고
  `<D:prop />`을 비워서 보낸다 - `calendar-data`를 요청해도 안 담아 준다.
  그 주소들로 `calendar-multiget`을 한 번 더 보내야 내용이 온다. 한 건씩 GET하지
  않는다 - 일정이 수십 개면 요청도 수십 개가 된다
- **네이버는 `<C:expand>`를 지원하지 않는다.** 반복 일정이 원본 그대로 오고,
  그대로 두면 매년 오는 생일이 시작일에만 찍힌다. 그래서 **우리가 편다.**
  원본과 어긋나지 않는 이유는 네이버가 필요한 걸 다 주기 때문이다 - 규칙(`RRULE`),
  뺀 날(`EXDATE`), 그 회차만 고친 별도 줄(`RECURRENCE-ID`). 셋을 다 쓰면
  "그 주만 옮김"과 "그 회는 취소"가 그대로 반영된다.
  **모르는 규칙은 손대지 않는다**(`BYSETPOS` 같은 것). 몇 건인지 세어 화면에 적는다 -
  조용히 틀린 날에 찍는 것이 제일 나쁘다
- **네이버는 한 파일에 여러 일정을 담아 보낸다.** 주소 목록은 기간으로 걸러져 와도
  그 내용에는 기간 밖 일정이 딸려 온다. 1964년부터 2043년까지 369건이 들어온 적이
  있다. **우리 쪽에서 기간으로 한 번 더 거른다**
- **네이버는 iCalendar 본문을 CDATA로 감싼다.** 안 벗기면 첫 줄이 안 맞아 통째로
  0건이 된다
- **네이버는 앱 비밀번호를 받는다.** 네이버에는 OAuth가 없고 CalDAV Basic 인증뿐이다.
  2단계 인증 안에서 만드는 앱 비밀번호는 캘린더에만 쓰이고 따로 폐기된다.
  계정 비밀번호를 받으면 우리 DB에 든 값이 메일·페이까지 여는 열쇠가 되고,
  여기가 뚫렸을 때 캘린더 유출로 끝나지 않는다
- **자격을 저장하기 전에 한 번 붙어본다.** 틀린 값을 넣어두면 하루 뒤 동기화에서야
  알게 되고, 그때는 무엇을 잘못 넣었는지 기억나지 않는다
- **CalDAV 파싱에서 네임스페이스 접두어를 고정하지 않는다.** 서버마다 `d:`, `D:`,
  `dav:`를 섞어 쓴다. 고정하면 조용히 빈 결과가 된다 - `IN (?)`에 배열을 넣는 것과
  같은 종류의 실패다

### 데이터

- **금액은 전부 원 단위 정수(`BIGINT`)로 저장한다.** 부동소수를 쓰지 않는다.
  계획 화면만 만원으로 표시한다(`71.42` → `714200`). 만원 입력은 100원 단위까지만
  표현되고 엑셀도 그 정밀도였다. 표시는 `lib/money.ts` 한 곳에서 만든다
- **`DATE`는 문자열로 읽는다**(`dateStrings: ['DATE']`). `Date`로 변환되면 풀의
  `timezone: 'Z'` 때문에 UTC 자정이 되고, 로컬로 포맷할 때 날짜가 하루 밀린다
- **월은 `'YYYY-MM'` 문자열**이다. 문자열 비교가 곧 월 비교이고(`'2026-08' <= '2026-09'`)
  타임존이 끼어들 여지가 없다. "오늘"은 서버에서 KST로 만든다
- **월별 스냅샷을 만들지 않는다.** 이게 설계의 중심 결정이다. 엑셀처럼 매월 행을
  복사해 두면 항목 이름을 고칠 때 과거 12개월이 다 따라 바뀌거나 안 바뀌거나를 매번
  결정해야 한다. 대신 항목 정의 + 시점별 금액(`plan_amount`) + 그 달의 예외
  (`plan_override`)로 둔다
- **계획 금액을 바꾸면 그 달부터 적용된다.** 정의 하나를 고치면 지난 달 화면까지 따라
  바뀌고, 그 달만 고치면 다음 달에 또 고쳐야 한다. `이 달만 수정`과 `건너뛰기`만
  `plan_override`로 그 달에 한정된다
- **`plan_amount`와 `plan_override`를 한 테이블에 섞지 않는다.** "이 달부터"와
  "이 달만"은 다른 뜻이고, 섞으면 조회할 때 어느 쪽 의도였는지 알 수 없다
- **`entry`에 `kind`가 없다.** 유동 생활비 기록은 전부 지출이다. 수입은 계획의 몫이다.
  지금 넣으면 화면에 지출/수입 토글이 생기고 그게 입력을 한 탭 늘린다. `category`도 같다
- **삭제 대신 아카이브/숨김/소프트 삭제.** 지난 기록이 참조하고 있다
- **상환표를 계산하지 않는다.** 은행 표를 그대로 넣는다. 원리금은 회차마다 조금씩
  늘어나는 체증식이고 이자는 구간 일수에 따라 흔들린다. 계산기를 만들면 은행 표와
  몇 원씩 어긋나고 그 차이를 설명할 방법이 없다
- **지급 수단은 계획과 기록이 같은 목록을 쓴다.** 기록의 기본값은 계획의 유동 예산
  그룹이 쓰는 수단이다. 쓰는 곳이 있는 수단은 지울 수 없다
- **잠금을 만들지 않는다.** 두 사람이 같은 항목을 동시에 고칠 수 있지만 둘뿐이라
  확률이 낮고 잠금 UI가 입력을 느리게 한다. 나중 쓰기가 이긴다. 대신 모든 페이지가
  동적이라 열 때마다 최신값을 읽고, 삭제는 되돌릴 수 있다
- **쓰기는 전부 서버 액션이다.** API 라우트를 만들지 않는다 - CloudFront 기본 동작이
  `AllViewer` 원본 요청 정책이라 POST와 쿠키가 그대로 넘어간다

## 데이터 모델

열한 테이블. DDL은 `migrations/*.sql`이 원본이고 여기에는 이유만 적는다.
모든 테이블에 `book_id`가 있고 전부 `1`이다 - 부부가 같은 가계부를 보는 것이
요구사항이라 사용자별로 쪼개지 않는다. 나중에 쪼갤 여지만 남긴 자리다.

| 축 | 테이블 | 메모 |
|---|---|---|
| 계획 | `method` | 지급 수단. 계획과 기록이 함께 쓴다 |
| 계획 | `plan_group` | 대분류. `is_variable`이 유동 생활비 그룹이고 그것이 일 기록의 예산이다 |
| 계획 | `plan_item` | 세부항목. 반복되는 계획의 **정의**다. 월별 인스턴스를 만들지 않는다 |
| 계획 | `plan_amount` | `(item_id, from_ym)`. "이 달부터 이렇게 낸다" |
| 계획 | `plan_override` | `(item_id, ym)`. "이 달만" + 건너뛰기 |
| 기록 | `category` | 일곱 개. `is_fallback`인 `기타`는 숨길 수 없다 |
| 기록 | `entry` | 유동 생활비 지출. `occurred_on`은 달력 날짜고 시각을 넣지 않는다 |
| 대출 | `loan` | 상환표가 없는 대출도 있다(만기일시상환) |
| 대출 | `loan_schedule` | 은행 표와 1:1. PK는 회차, `ym`에 UNIQUE |
| 일정 | `calendar_source` | 어디서 끌어오는가. 사람마다 한 줄. `credential`은 구글 refresh token |
| 일정 | `calendar_event` | 가져온 일정. 소프트 삭제도 `updated_at`도 없다 - 기간을 통째로 갈아끼운다 |

어떤 달의 금액이 정해지는 순서.

```
그 달의 '이 달만' 예외 > 그 달까지의 최신 변경값(plan_amount) > 최초 금액(plan_item.amount)
```

- **`pay_day = 32`가 말일이다.** 별도 컬럼을 두지 않는다 - 2월 30일 같은 값을 막으려면
  어차피 애플리케이션 검증이 필요하고, 값 하나로 끝나는 표현이 인덱스와 정렬에 편하다
- **적립 누적은 달을 한 달씩 훑어서 더한다.** 기본금액 × 개월로 줄여 쓰던 계산은
  금액에 시점이 생긴 뒤로 맞지 않는다
- **종료월이 지난 항목은 자동으로 사라진다.** 대가로 항목을 지우면 과거 달에서도
  사라지므로 삭제 대신 `archived_at`을 쓴다
- **`loan_schedule.ym`에 UNIQUE를 걸고 계획 조회는 이걸로 한다.** 회차는 1부터 세지만
  계획은 "2026년 8월"을 묻는다. 월 2회 상환 상품이 들어오면 이 UNIQUE가 먼저 깨지면서
  알려준다 - 조용히 잘못된 값을 제안하는 것보다 낫다
- **`entry` 인덱스에서 `deleted_at`이 가운데 있는 것은 의도다.** 모든 조회가
  `deleted_at IS NULL`을 항등 조건처럼 달고 있어 앞쪽에 와야 범위 조건인
  `occurred_on`이 인덱스를 계속 탄다
- 상환표 임포트는 파일 업로드를 만들지 않는다 - 스프레드시트 범위를 복사해 붙이는 게
  더 빠르다. 병합된 헤더는 첫 칸이 정수인 줄만 골라서 건너뛰고, 저장 전에 파싱 결과를
  먼저 보여준다(N건, 기간, 첫 행·마지막 행)

## 하지 않기로 한 것

| 안 하는 것 | 이유 |
|---|---|
| 계좌·카드 연동, 문자 파싱 | 자격도 없고 손입력이 컨셉이다 |
| 고정비·원리금의 일별 기록 | 계획으로 관리한다. 금액과 날짜가 이미 정해져 있다 |
| 자산·순자산 관리, 투자 | 대출 잔액까지만 본다 |
| 상환표 자동 계산 | 주어진 표를 그대로 넣는다 |
| 영수증 사진 | S3가 붙는다. 인프라를 늘리지 않는다 |
| 다중 통화, 네이티브 앱, 푸시 | 원화·모바일 웹 |
| 사용자 테이블 | 둘뿐이다. 허용 목록을 환경변수에 둔다 |
| 실시간 갱신 | 상대가 방금 넣은 기록은 새로고침해야 보인다 |
| 배치 | 없다 |

## 열어둔 것

1. **적립형 항목의 목표액.** `accrues`는 누적액만 보여준다. "300 모아서 상환" 같은
   계획이 생기면 `target` 컬럼과 진행률이 붙을 자리다
2. **월 2회 상환 상품.** `uk_sched_ym`이 그걸 막는다. 그런 대출이 생기면 계획 참조
   방식(월 → 회차)을 다시 정해야 한다. 지금은 막아두는 쪽이 안전하다
3. **30일 지난 소프트 삭제 정리.** 배치를 만들지 않기로 했으므로 쌓인다
4. **`book_id`를 실제로 쓰는 시점.** 하드코딩 `1` 제거만 하면 된다
5. **페이지네이션.** 지금은 OFFSET. 쌓이면 `(occurred_on, id)` 커서로 간다
6. **그래프.** 카테고리별 비중 · 월별 추이 · 대출 잔액 추이 셋이 남았다
7. **아내(네이버) 캘린더.** 네이버는 구독형 iCal 주소를 발급하지 않는다. 일회성
   `.ics` 내보내기뿐이라 자동으로 못 끌어온다. **CalDAV는 열려 있으므로** 그 길로
   가야 하고, 그러려면 네이버 계정 자격이 서버에 저장돼야 한다. `calendar_source`의
   `provider`에 `naver`와 `credential` 자리를 이미 비워뒀다
8. **구글 앱 인증(verification).** 지금은 미인증 프로덕션이라 동의 화면에
   "확인되지 않은 앱" 경고가 뜨고 사용자 한도가 100명이다. 두 사람만 쓰므로 받지 않았다

<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
